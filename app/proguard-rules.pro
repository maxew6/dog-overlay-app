# Add project specific ProGuard rules here.
# This app ships with minifyEnabled=false by default (kept tiny/simple),
# so these rules are not active unless you flip isMinifyEnabled to true
# in app/build.gradle.kts.
